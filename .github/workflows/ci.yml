name: CCR CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: # allow manual trigger from the Actions tab

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
        node: [24]
    env:
      CI: "true"
    steps:
      - uses: actions/checkout@v4

      # Diagnostic first so any failure has useful context.
      - name: Runner diagnostics
        run: |
          node --version || true
          git --version
          pwd

      - uses: pnpm/action-setup@v4
        with:
          version: 11.2.2
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Environment
        run: |
          node --version
          pnpm --version

      - name: Install (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Project-reference validation (R0.1.1)
        run: node scripts/check-project-references.mjs

      - name: Architecture boundary check (R0.1.8)
        run: node scripts/architecture-check.mjs

      - name: Build + typecheck (from clean dist)
        run: |
          powershell -Command "Get-ChildItem packages -Recurse -Directory -Filter dist -ErrorAction SilentlyContinue | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue; Get-ChildItem packages -Recurse -Filter *.tsbuildinfo -ErrorAction SilentlyContinue | Remove-Item -Force -ErrorAction SilentlyContinue"
          pnpm build

      - name: Unit + integration tests
        run: pnpm test

      - name: Gate evidence consistency (R0.1.10)
        run: node scripts/verify-gates.mjs

      - name: Stdio subprocess integration tests
        run: node --experimental-strip-types --test packages/mcp-adapter/test/stdio-subprocess.test.ts

      - name: VCS tests (git required)
        run: node --experimental-strip-types --test packages/vcs/test/vcs.test.ts
